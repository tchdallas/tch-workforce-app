// Canonical public URL of the deployed app. Used for links inside emails
// (invites, password resets) so they open the real app on any device —
// never localhost or whatever URL the sender happened to be on.
//
// When the custom domain goes live, change this ONE line to:
//   'https://workforce.texascardhouse.com'
export const APP_URL = 'https://tch-workforce.vercel.app';
